# Critique Round 2

(Used only in `deep` mode, after `construct_rewrite_1.md` is written. Standard mode runs only round 1.)

Construct under critique: TODO (name + one-line definition, **after round 1 rewrite**)
Round started: TODO (date)
Reference: changes from round 1 documented in `construct_rewrite_1.md`.

## Round-1 Carry-Over

Briefly note which angles failed in round 1 and what the round-1 rewrite changed. Round 2 will check whether the rewrites stuck and whether new angles fail on the revised draft.

- Angle [N] (round 1 fail): TODO — round-1 fix was [...]. Round-2 status: pass / still fail / now over-correcting.
- ...

## Summary

| Angle | Round 1 | Round 2 | Severity (round 2) |
|---|---|---|---|
| 1 Headline competition | TODO | TODO | TODO |
| 2 Construct vs neighbors | TODO | TODO | TODO |
| 3 Ladder vs design-space | TODO | TODO | TODO |
| 4 Formative inquiry | TODO | TODO | TODO |
| 5 Ground-truth stability | TODO | TODO | TODO |
| 6 Deployment cost honesty | TODO | TODO | TODO |
| 7 Demand characteristic | TODO | TODO | TODO |
| 8 Venue split-personality | TODO | TODO | TODO |

Failing angles in round 2: TODO (count)
Pass condition (≤ 1 failing): TODO

If pass condition triggered, rename to `critique_round_2_passed.md` and proceed to H4. If failing angles ≥ 2, fill per-angle findings below and write `construct_rewrite_2.md`.

---

## Per-Angle Findings (Round 2)

Use the same four-field shape as round 1: trigger evidence (quoted from `construct_rewrite_1.md`) / fail finding / fix / owner section in `construct_rewrite_2.md`.

Round 2 is typically tighter than round 1 — most angles should pass. The angles most likely to *still* fail in round 2:

- **Angle 2 (Neighbor differentiation)** — round 1 added the table but rows may still be definitional rather than dimensional.
- **Angle 8 (Venue split-personality)** — round 1 picked a primary, but the construct framing may still over-serve the pivot venue.
- **Angle 1 (Headline)** — sometimes the round-1 fix introduces a *new* competing headline through the rewrite.

If round 2 finds zero new fails and no carry-over fails, write a one-paragraph "round 2 closed" summary at the end and proceed to H4.

### Angle 1 — Headline Competition (round 2)

**Trigger evidence (quoted from rewrite_1):**

> TODO

**Fail finding (if any):**

TODO

**Fix:**

TODO

**Owner section in rewrite_2:**

TODO

### Angle 2 — Construct vs Neighbor Concepts (round 2)

(Same shape — fill if fail.)

### Angle 3 — Ladder vs Design-Space (round 2)

(Same shape.)

### Angle 4 — Formative Inquiry (round 2)

(Same shape.)

### Angle 5 — Ground-Truth Stability (round 2)

(Same shape.)

### Angle 6 — Deployment Cost (round 2)

(Same shape.)

### Angle 7 — Demand Characteristic (round 2)

(Same shape.)

### Angle 8 — Venue Split-Personality (round 2)

(Same shape.)

---

## Rewrite Hand-Off

The following sections of `construct_rewrite_2.md` must change:

- TODO: § name — to absorb fix from angle [list]

If topic_lock.yaml was updated in round 1 and round 2 changes scope further:

- [ ] Re-update `config/topic_lock.yaml`.
- [ ] Append another row to `reports/05_topic_drift_warning.md`.

## Closing the Loop

After round 2 (whether it passes or fails), Module H proceeds to H4 (venue fit decision). Do not run a round 3 unless the user explicitly requests it — diminishing returns set in fast.
